# L2 fiducial Y_5R500c Backfill Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add official SOAP `Y_5R500c_Mpc2` to the canonical L2p8_m9 lightcone0 q-from-map catalogue without changing any existing field.

**Architecture:** A small backfill script separates value lookup from line-preserving CSV output. Values are fetched by `(snap, soap_index)`; the writer appends one field to each original data line and performs an atomic replacement only after validation.

**Tech Stack:** Python, NumPy, pandas, hdfstream, pytest.

## Global Constraints

- L1 already contains the column and remains unchanged.
- L2 identity is `(snap, soap_index)`.
- The SOAP field is `SO/5xR_500_crit/ComptonY` and output name is `Y_5R500c_Mpc2`.
- Existing CSV field text must be preserved exactly.
- The canonical file must only change via validated atomic replacement with a recoverable hard-link backup.

---

### Task 1: Line-preserving backfill utility

**Files:**
- Create: `scripts/backfill_l2_y5r500.py`
- Create: `tests/test_backfill_l2_y5r500.py`

**Interfaces:**
- Consumes: a CSV pathname and a row-aligned NumPy array of SOAP values.
- Produces: `append_column_preserving_rows(source, temporary, column, values)`.

- [ ] **Step 1: Write failing tests**

Test a commented CSV fixture and assert that comments are unchanged, the header gains `Y_5R500c_Mpc2`, every old row is an exact prefix of the corresponding new row, and duplicate target columns are rejected.

- [ ] **Step 2: Verify RED**

Run `pytest -q tests/test_backfill_l2_y5r500.py`; expect import failure because the utility does not exist.

- [ ] **Step 3: Implement the line-preserving writer**

Stream the source and append one `%.17g` value per data row. Validate row count, finite non-negative values, and exact old-row prefixes before returning.

- [ ] **Step 4: Verify GREEN**

Run `pytest -q tests/test_backfill_l2_y5r500.py`; expect all tests to pass.

### Task 2: Official SOAP lookup and canonical update

**Files:**
- Modify: `/rds/rds-lxu/flamingo/L2p8_m9/lightcone0/catalogues/halo_catalogue_M500c_5e13_zlt3_L2p8_m9_yang26rot_qfrommap.csv`

**Interfaces:**
- Consumes: official SOAP snapshots addressed by L2 `(snap, soap_index)`.
- Produces: the canonical CSV with one appended `Y_5R500c_Mpc2` column and a `.pre_y5r500.bak` hard-link backup.

- [ ] **Step 1: Probe SOAP lookup**

Read two known L2 identities and require finite non-negative `SO/5xR_500_crit/ComptonY` values.

- [ ] **Step 2: Fetch all values**

Read only `snap, soap_index` from the canonical CSV, group by snapshot, and fetch the official field into a row-aligned NumPy array.

- [ ] **Step 3: Write and validate the temporary CSV**

Use `append_column_preserving_rows`, then reopen the temporary file and verify row count, identity digest, column presence, and finite non-negative values.

- [ ] **Step 4: Publish atomically**

Create the hard-link backup if absent and replace the canonical pathname with the validated temporary file.

- [ ] **Step 5: Verify final state**

Confirm L1 and L2 both expose `Y_5R500c_Mpc2`; confirm L1 checksum is unchanged and L2 identity/old-row digests match the pre-update values.

