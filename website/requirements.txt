# Website-specific dependencies. Everything else (fastapi, uvicorn, healpy, pandas,
# matplotlib, google-cloud-storage, pyarrow) is already in cmbagent_env.
fastapi>=0.110
uvicorn>=0.29
duckdb>=1.1
httpx>=0.27  # TestClient transport for tests
